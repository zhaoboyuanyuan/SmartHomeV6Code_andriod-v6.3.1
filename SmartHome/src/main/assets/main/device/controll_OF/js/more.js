/**
 * Created by Veev on 2017/6/9.
 */
var moreConfig;
function getMoreConfig(devID) {
    var config = {
        "deviceID": devID,
        "data": []
    };

    moreConfig = config;
    return config;
}