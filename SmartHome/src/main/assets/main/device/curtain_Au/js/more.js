/**
 * Created by Veev on 2017/6/9.
 */
var moreConfig;
function getMoreConfig(deviceID) {
    var config = {
        "deviceID": deviceID/*,
        "data": [
            {
                "group": "log",
                "enableWithEnterType":"account",
            }
        ]*/
    };

    moreConfig = config;
    return config;
}